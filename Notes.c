// start from 'A'  and move through room (increment array)
// if next space = * turn or stop,
// Place "Letter" in empty space
// if next space = "LETTER" and if letter is less than current letter stop
// if space is 25+ steps, place "Z" in space
